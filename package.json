{
  "name": "shesheild",
  "version": "1.0.0",
  "description": "Full-stack SheShield Safety App",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run server --prefix backend\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run server --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
